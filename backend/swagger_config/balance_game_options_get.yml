summary: "balance_game_responses_get"
description: "밸런스 게임 유저 응답 %로 가져오는 api"
consumes:
- "application/json"
produces:             
- "application/json"
parameters:
- in: path
  name: balance_game_options_id
  description: "밸런스 게임 옵션 id를 parameter로 받는다"
  required: true
  type: integer
  default: 1
responses:
  405:
    description: "Invalid input"